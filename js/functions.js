// functions = Define code once, and use it many times.
//            To perform some code, call the function name.
// Recommended to make function name as a verb/action 
// 


// parameter //
// puts a value into a function //
// can have more than one parameter //

function calculateTax(cost, taxPercent) {
	console.log(cost * taxPercent);
}

calculateTax(2000, 0.1); // 2000 is called the argument or value passed 
200