// return = returns a value back to the place
//          where you invoked a function
// A return statement ends the function immediately after 

function square(x) {
  return x * x;
}
const demo = square(3);
// demo will equal 9